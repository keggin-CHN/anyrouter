# AnyRouter API 挂机保活与技术交接文档 (Go 原生轻量版 v2.6)

## 1. 概述与核心定位

本项目为 **AnyRouter 平台专属的高可用客户端服务体系**，适配国内网络环境下的特殊路由与高并发排队机制。
核心交付物为 **Go 纯净单文件桌面 GUI 自动挂机排队保活工具 (`anyrouter-keeper.exe`)**，已彻底去除 Python 运行时依赖，文件体积缩减 80%（从 38.5MB 降至 7.37MB），解决频繁排队、会话掉线与限流难题。

---

## 2. 关键运行前提：10808 本地代理说明

> [!IMPORTANT]
> **关于网络连接的特别强调**：
> - 直连 `anyrouter.top` 会触发 401 拦截或网络超时阻断。
> - **在本地环境下，必须通过 `http://127.0.0.1:10808` 代理通道连接**。
> - 当前挂机客户端与 Go 底层 SDK 均已**默认内置绑定 `http://127.0.0.1:10808`**，开箱即用，无需额外网络配置。

---

## 3. 项目架构与组件清单

```text
c:\code\anyrouter\
├── anyrouter-keeper.exe      # [核心交付物] Windows 原生桌面 GUI 挂机保活客户端 (双击直接弹出桌面界面，无黑框)
├── web\                      # [前端源码目录] 可交互式前端完整源码
│   └── index.html            # 现代化暗色主题监控面板 (含凭据池、3大Tab、监控矩阵、题库编辑器、实时日志)
├── keeper_config.json        # 挂机工具本地持久化配置文件 (自动双向同步 Key 列表、题库、参数与模型)
├── main.go                   # Go 程序总入口 (嵌入 WebView2 原生桌面窗口与本地服务引擎)
├── pkg\                      # 核心 Go 业务模块
│   ├── client\               # AnyRouter 客户端通信库 (SSE 流解析、Codex / Claude 深度协议伪装)
│   ├── keeper\               # 核心调度矩阵 (状态机调度、两阶段保活巡航、桌面通知、日志分发)
│   └── web\                  # 本地 Web 服务与 REST API (服务前端交互与状态同步)
├── tests\                    # 端到端集成测试与单元测试套件
├── go.mod                    # Go 模块声明
└── 交接文档.md               # 本文档
```

---

## 4. 核心交付物：可交互式桌面 GUI 客户端 (`anyrouter-keeper.exe`)

### 运行方式

直接双击运行：
```text
C:\code\anyrouter\anyrouter-keeper.exe
```
- **真正的原生桌面应用程序**：编译已开启 `-H windowsgui`，**双击无任何黑框控制台闪烁**。
- **直接弹出 1120×840 独立桌面窗口**：基于 Windows 内置轻量 WebView2 原生渲染，具有独立的窗口边框、最小化、最大化和关闭按钮。
- **与 Python 原版 1:1 像素级复刻**：保留深色高颜值暗黑质感、实时心跳指示灯、多 Key 换行输入、三 Tab 监控面板及底部高亮日志。
- **跨平台 Web 访问**：程序启动后同时在本地监听 `http://127.0.0.1:28888`，也可在浏览器中直接打开进行多端可视化监控。

---

## 5. 前端源码与自定义修改 (`web/index.html`)

前端所有交互逻辑、样式与控件位于：
```text
c:\code\anyrouter\web\index.html
```
- **热修改即时生效**：客户端启动时优先读取本地 `web/index.html`。您修改该文件后，在桌面窗口中按 `F5` 或刷新即可立即看到修改效果，无需重新编译 Go 代码！
- **脱机单文件保障**：即使分发单文件 `.exe` 到没有 `web/` 目录的新电脑，程序也会自动回退至二进制内置的嵌入式前端，真正实现单文件零依赖。

---

## 6. 核心功能与操作说明

1. **多 API Key 并发支持**：
   - 顶部提供凭据池多行文本框，**一行填入一个 API Key**，支持一键剪贴板粘贴与自动去重。
   - 界面实时高亮显示当前有效 Key 数量（例如：`已识别 2 个 Key`）。
2. **100 道极简预设测活题库（随机轮换）**：
   - 内置 100 道响应快（<1s）、消耗极低 Token 的经典提问（包含算术、单字指令、常识、微翻译、编程与逻辑等）。
   - **支持随机轮换**：每次尝试排队与定期测活均独立随机抽题，彻底防止平台缓存或判定固定自动化特征。
   - **界面完全可调**：在【⚙ 全参数高级配置与题库】面板中可自由增删修改题目，并提供【↺ 恢复 100 道预设题库】按钮。
3. **所有配置文件参数在界面全可视化调节**：
   - **Tab 1: 📊 多 Key 并发监控矩阵 (实时状态)**：展示每个 `Key × 模型` 通道的卡片（含 Key 徽标、当前阶段徽标、实时轮次 `#X/Y`、最近随机提问与响应文本）。
   - **Tab 2: 📋 模型选择 (可多选)**：多选目标模型，显示 Codex / Claude Code 协议分类徽标，支持全选/全不选/恢复推荐。
   - **Tab 3: ⚙ 全参数高级配置与题库**：
     - `每轮尝试次数`（默认 5 次）
     - `轮内单次间隔`（默认 5 秒）
     - `轮末冷却等待`（默认 30 秒，支持秒级动态倒计时）
     - `测活巡航周期`（默认 30 分钟）
     - `测活重试次数`（默认 3 次）
     - `本地代理通道`（默认 `http://127.0.0.1:10808`）
     - `随机题库轮换开关` 与 100 道题库编辑器
   - 点击【💾 保存配置】即时保存到 `keeper_config.json`，下次启动自动生效。
4. **两阶段自动化保活工作流**：
   - **阶段一【用不了】**：
     - 单次尝试由守护 Goroutine 直接驱动，每轮发起 5 次尝试（间隔 5 秒）。
     - **一旦出字能够响应**：立即将该通道无缝自动切换到阶段二，控制面板卡片实时转为绿色【能用】就绪状态。
   - **阶段二【能用】**：
     - 30 分钟定期测活（随机提问）。
     - **“能用就不管”**：测活正常时静默重置 30 分钟倒计时；若测活超时或失效，立即自动退回阶段一重新排队挤入。
5. **精选 3 大核心保活模型**：
   - 全面收敛并专注支持：`gpt-6-astra`、`claude-opus-4-8`、`claude-fable-5-1`。
6. **状态栏常驻与静默无扰守护**：
   - 应用程序自带专属路由器脉冲图标（嵌入 `.exe` 与任务栏、托盘）。
   - 点击右上角叉号（✕）自动隐藏并转入后台**静默保活**，**彻底去除流氓通知弹窗与系统提示音**，安静守护不打扰。
   - 双击托盘小图标可随时重新唤出控制面板；右键菜单提供【🖥 打开主控制面板】与【🛑 彻底退出程序】。
