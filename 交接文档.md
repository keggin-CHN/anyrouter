# AnyRouter API 挂机保活与技术交接文档

## 1. 概述与核心定位

本项目为 **AnyRouter 平台专属的高可用客户端服务体系**，适配国内网络环境下的特殊路由与高并发排队机制。
当前核心交付物为 **单文件桌面 GUI 自动挂机排队保活工具 (`anyrouter-keeper.exe`)**，解决频繁排队、会话掉线与限流难题。

---

## 2. 关键运行前提：10808 本地代理说明

> [!IMPORTANT]
> **关于网络连接的特别强调（前任文档误区澄清）**：
> - 前任文档在海外 AWS EC2 云主机测试时使用了 `proxy=None`（直连模式），这在**国内网络环境下完全不可行**（直连 `anyrouter.top` 会触发 401 拦截或网络超时阻断）。
> - **在本地环境下，必须通过 `http://127.0.0.1:10808` 代理通道连接**。
> - 当前挂机客户端与 Python 底层 SDK 均已**默认内置绑定 `http://127.0.0.1:10808`**，开箱即用，无需额外网络配置。

---

## 3. 项目架构与组件清单

```
c:\code\anyrouter\
├── anyrouter-keeper.exe   # [核心主推] Windows 单文件桌面 GUI 自动挂机排队保活工具 v2.5 (双击即用)
├── gui_keeper.py          # 挂机保活客户端桌面 GUI 源码 (Tkinter + 现代化暗色三 Tab 监控面板)
├── keeper_config.json     # 挂机工具本地持久化配置文件 (自动记忆多 Key 列表、题库、参数与模型)
├── anyrouter_client.py    # Python 专用底层客户端 SDK (支持深度伪装与自动排队)
├── requirements.txt       # Python 依赖项
├── README.md              # Python SDK 说明
└── 交接文档.md            # 本文档
```

---

## 4. 核心交付物：多 Key 自动挂机排队保活客户端 (`anyrouter-keeper.exe` v2.6)

### 运行方式
直接双击运行：
```text
C:\code\anyrouter\anyrouter-keeper.exe
```
单文件免安装，自带轻量运行时，自动载入并双向同步 `keeper_config.json`。

### 核心功能与操作说明
1. **多 API Key 并发支持**：
   - 顶部提供凭据池多行文本框，**一行填入一个 API Key**，支持一键剪贴板粘贴与自动去重。
   - 界面实时高亮显示当前有效 Key 数量（例如：`已识别 2 个 Key`）。
2. **10 大极简预设测活题库（随机轮换）**：
   - 内置 10 道响应快（<1s）、消耗极低 Token 的经典提问：
     1. `1+1`
     2. `用一个字回答：好`
     3. `输出一个数字: 8`
     4. `2*3等于几`
     5. `Hi, reply 1 word.`
     6. `请回复'pong'`
     7. `3+5等于几`
     8. `ping`
     9. `100-1等于多少`
     10. `从1数到3`
   - **支持随机轮换**：每次尝试排队与定期测活均独立随机抽题，彻底防止平台缓存或判定固定自动化特征。
   - **EXE 界面完全可调**：在【⚙ 全参数高级配置与题库】面板中可自由增删修改题目，并提供【↺ 恢复 10 大经典预设题库】按钮。
3. **窗口关闭行为决策（叉掉窗口时提供选择）**：
   - 当点击窗口右上角关闭按钮（✕）时，默认弹出交互确认窗口：
     - **【📥 最小化到系统托盘】**：窗口收起至屏幕右下角托盘，后台所有 Key 与模型的排队/测活通道**继续无缝运行**，并弹出提示气泡。
     - **【🛑 彻底退出程序】**：安全停止所有挂机守护线程，清理托盘图标并终止进程。
     - **【取消】**：返回主面板。
     - **【记住我的选择】**：勾选后下次点击叉掉将自动执行所选动作。
   - 可在【⚙ 全参数高级配置】中的【点击关闭行为】下拉框随时更改为【每次询问 (默认)】、【最小化到系统托盘】或【彻底退出程序】。
4. **系统托盘常驻支持**：
   - 托盘区域显示高对比度路由器脉冲图标。
   - **鼠标双击托盘图标**：一键重新唤出主控制面板。
   - **鼠标右键托盘图标**：弹出快捷菜单（打开主控制面板、查看挂机状态气泡、彻底退出程序）。
5. **所有配置文件参数在 EXE 内全可视化调节**：
   - **Tab 1: 📋 模型选择 (可多选)**：多选目标模型，显示 Codex / Claude Code 协议分类徽标。
   - **Tab 2: 📊 多 Key 并发监控矩阵 (实时状态)**：启动后自动切入，展示每个 `Key × 模型` 通道的卡片（含 Key 徽标、当前阶段徽标、实时轮次 `#X/Y`、最近随机提问与响应文本）。
   - **Tab 3: ⚙ 全参数高级配置与题库**：
     - `每轮尝试次数`（默认 5 次）
     - `轮内单次间隔`（默认 5 秒）
     - `轮末冷却等待`（默认 30 秒，支持秒级动态倒计时）
     - `测活巡航周期`（默认 30 分钟）
     - `测活重试次数`（默认 3 次）
     - `本地代理通道`（默认 `http://127.0.0.1:10808`）
     - `点击关闭行为`（下拉选择：每次询问 / 最小化托盘 / 彻底退出）
     - `随机题库轮换开关` 与 10 大题库编辑器
   - 点击【💾 保存配置】即时保存到 `keeper_config.json`，下次启动自动生效。
6. **两阶段自动化保活工作流**：
   - **阶段一【用不了】**：
     - 单次尝试由守护线程直接驱动，每轮发起 5 次尝试（间隔 5 秒）。
     - 每一单次尝试均递增计数：`第 1 轮 #1/5` -> `#2/5` -> `#3/5` -> `#4/5` -> `#5/5`。
     - 轮末进入 30 秒冷却倒计时，面板实时展示 `🟡 轮末冷却中 (29s 后开启第 2 轮)`，倒计时结束后自动启动下一轮。
     - **一旦出字能够响应**：立即在 **Windows 桌面右下角弹出绿色常驻浮窗卡片**（伴随系统提示音，多卡片自适应垂直向上堆叠），并自动无缝切换到阶段二。
   - **阶段二【能用】**：
     - 30 分钟定期测活（随机提问）。
     - **“能用就不管”**：测活正常时静默重置 30 分钟倒计时；若测活超时或失效，立即自动退回阶段一重新排队挤入。
